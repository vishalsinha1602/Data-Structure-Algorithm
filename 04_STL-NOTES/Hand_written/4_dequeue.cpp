#include <bits/stdc++.h>
using namespace std;

// First, solve the problem. Then, write the code

int main()
{

    // dequeue is basically a doubly ended queue with he featur of expansion and constraction at the both end

    // unlike queue and stack it allow to perform insertionand deleltion at the both end
    // implemented using doubly linked list

    deque<int> de;
    // all function are same as vector
    // begin , end ,rbegin,rend,insert,clear,size,swap

    return 0;
}